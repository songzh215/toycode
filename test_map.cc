#include <map>
#include <stdlib.h>
#include <string>
#include <stdio.h>

int main() {
    std::map<std::string, std::string> test_map;
test_map["abc"]
    if () {
        printf("no exist");
    }
    exit(0);
}
